<?php
if (!defined('WP_UNINSTALL_PLUGIN')) exit;
// Intentionally preserves settings/data on uninstall to protect user configuration.
// Administrators can use the Reset control before uninstalling if they want defaults.
